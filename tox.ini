[tox]
envlist = py26, py27, pypy, py33, py34, py35

[testenv]
usedevelop = true
deps =
    pytest
    pytest-cov
commands =
    py.test --cov=corral --cov-report=term-missing -vv

[testenv:coveralls]
passenv = TRAVIS TRAVIS_JOB_ID TRAVIS_BRANCH
deps = coveralls
commands =
    coverage combine
    coverage report
    coveralls
